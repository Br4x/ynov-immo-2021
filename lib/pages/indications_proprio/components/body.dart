import 'package:flutter/material.dart';
import 'package:ynov_immo/pages/indications_proprio/components/MainPageState.dart';

class Body extends StatelessWidget{
  @override
  Widget build(BuildContext context) => Scaffold(
        body:
        Center(child: MainPageState()),
      );
}
